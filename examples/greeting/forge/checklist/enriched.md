- [ ] Whitespace handling preserves the intended name.
  Trim surrounding whitespace and use `world` for empty or whitespace-only names.
  Preserve punctuation, Unicode, and spaces inside a nonblank name. Compare direct
  function calls with command-line use. Choose useful cases and record performed
  checks and their limits in the copied workspace.
